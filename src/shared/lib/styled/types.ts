export type ApplicationColors = {
  text: {
    primary: string;
    red: string;
    green: string;
    blue: string;
  };
  background: {
    primary: string;
    secondary: string;
  };
  button: {
    primary: string;
    secondary: string;
  };
  green: string;
  red: string;
  blue: string;
  lightBlue: string;
};
